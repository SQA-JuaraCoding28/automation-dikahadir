# =============================================================================
# Cucumber Runtime Properties
#
# NOTE: plugin, glue, and features are configured in TestRunner.java
# (@CucumberOptions) which is the single source of truth for runner config.
# Duplicating them here causes ambiguity — do not add them back.
# =============================================================================

# Disable the Cucumber online report publishing service
cucumber.publish.enabled=false
